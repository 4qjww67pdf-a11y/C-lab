/*program to find ther are of rectangle*/
#include<stdio.h>
int main()
{
	int l,b,area;
	printf("enter length and breadth of rectangle :");
	scanf("%d %d",&l ,&b);
	area=l*b;
	printf("the area of rectangle with length %d and breadth %d is %d",l,b,area);
	return 0;
	
}
